@model IEnumerable<CameraModel>

@{
    ViewData["Title"] = "All Cameras";
}

<h2>@ViewData["Title"]</h2>
<br/>

<div class="container">
    <div class="row">
        @foreach (var camera in Model)
        {
            @Html.Partial("_CameraCard", camera)
        }
    </div>
</div>
